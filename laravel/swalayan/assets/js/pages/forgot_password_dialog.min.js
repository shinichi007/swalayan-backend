Dashmix.onLoad((() => class { 
    static sweetAlert2() { 
        let e = Swal.mixin({ 
            buttonsStyling: !1, 
            target: "#page-container", 
            customClass: { 
                confirmButton: "btn btn-success m-1", 
                cancelButton: "btn btn-danger m-1", 
                input: "form-control" 
            } 
        });
        document.querySelector(".js-swal-success").addEventListener("click", (t => { 
            e.fire("Sukses", "Email konfirmasi terkirim, silahkan cek email anda dan konfirmasi alamat email anda", "success") 
        }))
    } 
    static init() { 
        this.sweetAlert2() 
    } 
}.init()));